require(['app','lib/backbone.localStorage.min'], function(app,LocalStorage){
    
    //import { LocalStorage } from 'backbone';
   // import app from 'app.min'; 

 app.collections.languages=Backbone.Collection.extend({
    initialize:function(){
     //   console.log("initializing languages collection")
    },

    model:app.models.language,
    localStorage:new LocalStorage("languages")
    // localStorage:new Backbone.BrowserStorage.local("languages")
    
})
return app.collections.languages;
  })