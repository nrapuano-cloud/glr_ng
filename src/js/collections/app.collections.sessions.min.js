app.collections.sessions=Backbone.Collection.extend({
	initialize:function(){
		//console.log("initializing sessions collection");
        },
	//model:app.models.token,
      model:app.models.session,
     
	sessionStorage: new app.models.session()
})