require(['app','lib/backbone.localStorage.min'], function(app,LocalStorage){
   
app.collections.tokens=Backbone.Collection.extend({
    initialize:function(){
        //console.log("initializing tokens collection");
    },
    model:app.models.token,
    // model:app.models.session LocalStorage
    //localStorage:new Backbone.LocalStorage("tokens"),
    localStorage:new  LocalStorage("tokens"),
   
})
return app.collections.tokens;
  })